# 页面验证

按本次用例验证用户任务是否真正完成，而不只看点击、请求或页面加载是否成功。在已授权的开发、测试环境中，用例所需的正常数据创建、修改和删除属于验证，应实际覆盖；这与修改源码修复问题不同。

沿约定的测试链路执行。遇到阻碍时，可以排除原授权内的操作或环境问题；仍无法继续就报告实际失败或阻塞。为绕过阻碍而模拟响应、注入成功状态或替换链路，不能证明原用例通过。

报告实际结果、关键证据及失败或阻塞发生的位置。模拟、局部验证与完整链路结果分别说明，让调用方看清哪些行为确实测过、哪些尚未验证。
