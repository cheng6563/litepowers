---
name: systematic-debugging
description: "通过复现、证据、数据流和单一假设定位未知故障的根因。Use when a bug, failing test, stack trace, or unexpected behavior is observed but the cause is not yet established; after root cause is known, use tdd for the fix."
---

# 系统化调试

乱试浪费时间还引入新 bug，快速打补丁掩盖真问题。

**铁律：动手修之前先定位根因。** 修症状 = 失败。

## 四阶段

逐阶段推进，前一阶段没完成不进下一阶段。

### 1. 定位根因（动手前必做）

- **认真读报错** — 别跳过，错误信息常常就含答案。读完整栈、行号、文件、错误码。
- **稳定复现** — 能可靠触发吗？步骤是什么？不能复现就再收集数据，别猜。
- **看最近改动** — git diff / 最近 commit / 新依赖 / 配置变更。
- **多组件系统先加埋点** — 系统有多层（请求→服务→数据库 等）时，在每个边界打日志（进什么、出什么、配置有没有透传），**跑一次拿证据看在哪一层断**，再去查那一层。
- **顺数据流回溯** — 坏值从哪来？谁传进来的？一路往上追到源头，在源头修，不在症状处修。

### 2. 找模式

- 找同代码库里类似的**能跑**的例子。
- 要照搬某个范式，就**完整读**参考实现，别只扫一眼。
- 列出能跑的和坏的之间**每一处差异**，别假设"这个不可能有影响"。

### 3. 假设与验证

- **单一假设**：明确写下"我认为 X 是根因，因为 Y"。
- **最小测试**：做能验证假设的最小改动，一次只动一个变量。
- **先验证再继续**：成了 → 阶段 4；没成 → 提新假设，**别在旧改动上叠新补丁**。
- **不懂就说不懂**，别装懂，去查或问。

### 4. 交接实现

根因、证据链和预期行为明确后，转 `skill:tdd` 建立失败证据并实施最小修复；完成声明前转 `skill:verification`。若连续三个单一假设都失败，停下来重审证据和架构，不再叠补丁。

出现无假设试改、一次改变多个变量或连续叠补丁时，撤销实验并回到阶段 1。

## 真没有根因时

系统化排查后确认确实是环境 / 时序 / 外部因素导致：记录排查证据，加合适的处理（重试 / 超时 / 明确报错）和监控日志。不能证明根因时如实标注未知，不要用“外部因素”提前结束调查。
